# 自定义素材目录（本机自用）

本目录已被 .gitignore 排除 —— **素材永不进 git，也不进部署产物**。

## 用法
1. 把提取的 PNG 放进本目录（可建子目录）
2. 复制 `mapping.example.json` 为 `mapping.json`，按宠物/敌人 id 填映射
3. 刷新页面即生效；缺图自动回退 Kenney/矢量皮肤

## 许可
放进本目录的素材版权归原厂商所有，仅限本机自用研究，严禁分发/商用/提交。
